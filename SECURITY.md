# Security

Report security issues privately to `santosh@agumbe.ai`.

Do not include access tokens, OAuth authorization codes, session cookies,
customer data, or other secrets in a public issue.

The plugin bundle contains workflow instructions and a remote MCP URL only.
Authentication is handled through the Agumbe OAuth flow.
